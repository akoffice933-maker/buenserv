import Link from 'next/link';
import {getTranslations} from 'next-intl/server';

export async function SiteFooter({locale}: {locale: string}) {
  const t = await getTranslations('footer');
  return <footer className="border-t border-black/8 bg-white"><div className="mx-auto grid max-w-7xl gap-8 px-5 py-10 sm:grid-cols-2 lg:grid-cols-4 lg:px-8"><div><p className="font-display text-xl font-extrabold">BuenServ</p><p className="mt-2 max-w-xs text-sm text-bs-muted">{t('tagline')}</p></div><div><p className="text-xs font-extrabold tracking-[.1em] text-bs-primary uppercase">{t('explore')}</p><div className="mt-3 grid gap-2 text-sm font-bold"><Link href={`/${locale}/categories`}>{t('categories')}</Link><Link href={`/${locale}/how-it-works`}>{t('how')}</Link><Link href={`/${locale}/providers`}>{t('providers')}</Link></div></div><div><p className="text-xs font-extrabold tracking-[.1em] text-bs-primary uppercase">{t('help')}</p><div className="mt-3 grid gap-2 text-sm font-bold"><Link href={`/${locale}/faq`}>FAQ</Link><Link href={`/${locale}/safety`}>{t('safety')}</Link><Link href={`/${locale}/contact`}>{t('contact')}</Link></div></div><div><p className="text-xs font-extrabold tracking-[.1em] text-bs-primary uppercase">{t('legal')}</p><div className="mt-3 grid gap-2 text-sm font-bold"><Link href={`/${locale}/legal/terms`}>{t('terms')}</Link><Link href={`/${locale}/legal/privacy`}>{t('privacy')}</Link><Link href={`/${locale}/legal/cookies`}>{t('cookies')}</Link></div></div></div><div className="mx-auto max-w-7xl border-t border-black/8 px-5 py-5 text-xs text-bs-muted lg:px-8">© 2026 BuenServ · Buenos Aires</div></footer>;
}
